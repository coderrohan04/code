class Solution {
    int missingNumber(int arr[], int N) {
      int expectedSum = N * (N + 1) / 2;
        int actualSum = 0;
        for (int num : arr) {
            actualSum += num;
        }
        return expectedSum - actualSum;
    }
    }
in this we have to find the missing element in any array
